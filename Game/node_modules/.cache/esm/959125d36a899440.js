_6a8‍.x([["printError",()=>printError],["toLower",()=>toLower],["checked",()=>checked]]);function printError(errors, campo) {
    let message
    if (errors) {
        errors.errors.forEach(error => {
            if (error.path === campo) {
                message = error.message;
            }
        })
    }
    return message;
}

       function toLower(value) {
    return value.toLowerCase();
}

       function checked(areaId, id) {
    if (id == areaId) {
        return 'checked';
    } else {
        return '';
    }
}
